/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from '../app.json';
import {name as appName} from '../app.json';

export default function Index() {


    return (

            <Text style="light" hidden={false}>A</Text>

    );
}